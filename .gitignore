# TLS keys and certificates — dev-only, never commit
certs/

# Client-side TOFU trust store (per-user identity key pins)
trusted_keys.json

# Encrypted identity + ratchet state (passphrase-protected, but still secrets)
ratchet_state_*.json
.ratchet-*.tmp

# Runtime
server.log
*.log

# Server-side offline message queue
cipherchat_server.db
*.db-journal
*.db-wal

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
